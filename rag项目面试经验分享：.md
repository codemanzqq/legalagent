# RAG 项目技术面试经验与答题思路分享：

## 1、自我介绍

**参考思路：**

分三部分讲：

- 个人情况介绍
  - 基本信息
  - 掌握的技术栈（写的就好想好怎么说）
- 做过哪些项目，用什么技术栈，最终效果（准确率，性能）
- 平时关注的技术，未来期望



## 2、项目介绍

**参考思路：**

- 先讲项目架构：记住先总后分，这里可以详细展开讲5-10分钟
  - 项目架构分为几层
  - 每一层分别实现什么？
  - 再把实现核心实现链路串起来讲一下（面试官能听懂，说明你对项目很熟悉）

## 3、高频问题

- 遇到最大的困难是什么？如何解决的？

  **参考思路：**

  - 可以用准确率/召回率不高来说，最后用了什么方法，效果从多少提高到了多少？

- 幻觉你是怎么解决的？

  **参考思路：**

  - 优化提示词
  - 换参数量更大的模型

- 压测指标多少？

  **参考思路：**

  - 并发
  - 首token
  - QPS/QPM
  - 吞吐量/秒

- 生成大模型用的是哪个？为什么用这个？

  **参考思路：**

  - 需要想介绍一下所用大模型的架构
  - 再对比几个同期的大模型
  - 突出选择合理性：场景适配，性能、资源（显卡）

- 有做了哪些优化？

  **参考思路：**

  - 混合检索
  - RRF融合算法
  - 重排模型

- 介绍下重排模型

  **参考思路：**

  Bge-rerank-large是一款高性能的重排序模型，用于在rag系统中对初步召回的候选文档进行精细化重排，以提升最终的排序结果。

  - 基于transformer的交叉编码器（cross-encoder）

  - 架构：它包含两个模型分别是xlm-reberta-base和xlm-reberta-large

  -  输入形式：将输入（query）和候选文档（document）拼接成一个序列（【CLS】 query【SEP】document【SEP】）

  - 输出是一个标量分数，表示query与候选文档的相关性

- 项目怎么部署的？用哪个框架？

  **参考思路：**

  - VLLM

- 项目部署用什么显卡？

  - 调API：想好是哪一家的，了解基本的参数
  - 自己部署：显卡型号，显存大小要能讲出来

## 4、项目外高频问题

- 介绍下transformer架构

- transformer计算公式

- 为什么现在的大模型都是基于transformer架构

  **参考思路：**

  核心是它的算法原理解决了传统rnn的核心痛点，适配大规模算力和产业需求，

  具体有以下几个方面：

  - 并行计算：传统rnn基于序列循环计算，每个时刻的输出依赖上一个时刻的状态，无法并行，数据量一大训练极慢；而transformer的注意力机制基于矩阵运算，每个位置的特征都能并行计算，大幅提升了训练效率。

  - 捕捉长距离依赖：rnn基于序列循环传递信息，长序列中早期信息逐渐衰减，无法捕捉长距离的语义关联；transformer的注意力机制中，每个token都直接与所有token计算注意力权重，任意两个位置的信息交互复杂度为O，解决了nlp领域基于“长文本建模”的痛点

  - 注意力机制的泛化性：多头注意力机制能同时学习更多的语义关联（语义，语法，实体关系），表达能力远超传统rnn单一的循环结构

- 介绍下VLLM框架？

  **参考思路：**

  Vllm的核心就是pageattention，是vllm提出的分块式kv cache管理机制，借鉴了操作系统分页机制，将kv cache分割成固定大小的页，将kv存储在统一的页池中，再通过page table将token映射到对应位置

- Kvcache介绍一下？

  **参考思路：**

  在计算注意力机制时缓存k和v，在计算下一个token时不需要重复计算，使注意力计算福复杂度从on方降低到on

- 讲一下连续批处理？

  **参考思路：**

  答：连续批处理的提出是因为传统的请求解码时需要整批里最长的请求完成后才能进入下一批，连续批处理的核心是将请求拆分为全量解码和单步增量解码阶段，来调度不同请求的步骤，从而实现动态提升有效批处理大小，提高GPU计算强度，避免短请求等待长请求。大幅提升模型在推理时的服务效率和吞吐能力

