# 启动与部署

按模块列出依赖与启动顺序；默认开发环境为 **Windows + 小皮 MySQL + Docker（Milvus/redis ）+ Conda**。

---

## 1. 运行环境与 Python 依赖

- **Python**：3.11.x（推荐）。  
- **Conda**（示例）：`conda create -n xiaoyi_py311 python=3.11 -y` → `conda activate xiaoyi_py311`。  
- **安装依赖**（在项目根目录）：

```bash
pip install -r requirements.txt
pip install -r requirements-dev.txt
```

可选：`pyproject.toml` 保留给 **uv** 或 IDE；日常以 `requirements.txt` 为准。勿在同一终端叠加 **conda base** 与项目 `.venv`，避免 PyTorch DLL 冲突。

---

## 2. MySQL（小皮或其它实例）

- 创建数据库（字符集 `utf8mb4`），用户名密码与 **`.env`** 中 `MYSQL_*` 一致。  
- 可选：执行 `scripts/init_mysql.sql`（若项目提供建库/账号脚本）。  
- 应用与离线脚本会在首次运行时 **`create_all`** 创建业务表，包括但不限于：  
  - **知识库**：`faq_tab`、`legal_tab`（离线 FAQ/PDF 入库）。  
  - **对话记忆**：`users_tab`（用户外部标识）、`his_chat_tab`（问答历史）；详见下文「对话记忆模块」。  
- **升级代码后若新增了表**：重启后端一次，由 `lifespan` 中的 `create_all` 自动补表（开发环境常用；生产环境建议评审后使用迁移工具）。

### 2.1 记忆模块依赖说明（无额外安装项）

- 记忆功能**仅依赖「已可用的 MySQL」**，**不需要**新增 Redis/Milvus 配置项，也**不需要**在 `.env` 里单独开关。  
- 若 MySQL 不可用，对话仍可走 RAG，但**无法**落库历史、**无法**按用户检索近期聊天记录。

---

## 3. Redis

- 默认 `redis://127.0.0.1:6379/0`，与 `.env` 中 `REDIS_URL` 一致。  
- 可使用 **Docker Compose** 中带起的 Redis，或本机独立安装。  
- 问答结果缓存 key **包含用户标识**（与 `user_external_id` 组合），避免多用户共用同一条缓存。

---

## 4. Milvus 与依赖（Docker）

- 进入 `deploy/`：`docker compose up -d`（具体服务名见该目录 `docker-compose.yml`）。  
- `.env` 中配置 `MILVUS_HOST`、`MILVUS_PORT`（常为 `19530`）。

**镜像拉取失败（Docker Hub EOF 等）**：在 Docker Desktop → Settings → Docker Engine 中合并 `deploy/docker-daemon-cn.json` 的镜像加速配置后 Apply & Restart；Compose 文件中已对部分镜像使用国内前缀。**etcd** 通常需从 `quay.io` 拉取；DaoCloud 路径不可写成 `docker.m.daocloud.io/quay.io/...`。终端勿长期设置 `HTTP_PROXY` 拉镜像；应在 Docker Desktop 的 Proxies 中配置。

---

## 5. 本地模型目录

将 **BGE-M3**、**bge-reranker-large** 权重置于项目根下 `models/bge-m3`、`models/bge-reranker-large`（或与 `.env` 中 `EMBEDDING_MODEL_PATH`、`RERANK_MODEL_PATH` 一致）。

---

## 6. 环境变量 `.env`

复制 `.env.example` 为 `.env`，至少填写：

| 类别 | 说明 |
|------|------|
| `DASHSCOPE_API_KEY` | 阿里云 DashScope（OpenAI 兼容）密钥，用于意图与生成。 |
| `MYSQL_*` | 数据库连接（**记忆模块读写用户表、聊天表同样走此连接**）。 |
| `REDIS_URL` | 缓存。 |
| `MILVUS_*` | 向量库。 |

进程启动时会 **清除当前 Python 进程内** 的 `HTTP_PROXY`/`HTTPS_PROXY`/`ALL_PROXY`，使调用 DashScope **直连**（`trust_env=False`）；与 Docker 拉镜像代理互不替代。

---

## 7. 对话记忆模块（users_tab / his_chat_tab）

本节描述「按用户保存聊天记录、并在特定问题时注入模型上下文」的行为与接口。**不涉及 Docker 额外容器**。

### 7.1 数据表职责

| 表名 | 作用 |
|------|------|
| `users_tab` | 一行代表一个终端用户；主键 `id`，业务字段 `external_id`（前端传入的稳定字符串，如 UUID），唯一索引。 |
| `his_chat_tab` | 一行代表一轮完整问答；关联 `user_id` → `users_tab.id`，字段含用户原始 `question`、助手完整 `answer`、`created_at`；删除用户时级联删除其历史。 |

### 7.2 何时读库、何时不写上下文

- **写入**：每次 `POST /api/chat/stream` 在 **SSE 正常结束**（未中途异常）后，将本轮「用户问题 + 助手完整拼接文本」写入 `his_chat_tab`（需传入 `user_external_id`）。  
- **读出（最多 10 条）**：仅当请求携带 **`user_external_id`**，且当前问题被 **`is_self_history_question`** 判定为「与自身聊天记录相关」（例如「我一共问了几个问题」「前 8 个问题问了什么」）时，才查询该用户最近 **10** 条历史，格式化后拼入大模型输入；**否则不查询历史表**，避免无关请求拖慢数据库与污染上下文。

### 7.3 API 与前端约定

| 项目 | 说明 |
|------|------|
| 请求体字段 | `message`（必填）；`user_external_id`（可选，最长 128 字符）。 |
| 不传 `user_external_id` | 不落库、不注入记忆；行为与旧版纯 RAG 一致。 |
| 前端默认实现 | `frontend/src/App.vue` 使用 `localStorage` 键 `xiaoyi_user_external_id` 存 UUID，首次访问 `crypto.randomUUID()` 生成并持久化。 |

### 7.4 与 RAG 流程的关系简述

- Redis 缓存键与 **`user_external_id` + 问题正文** 绑定，避免用户间串答。  
- 若本轮注入了记忆片段，会跳过「非专业问题 → 仅引导」的拦截（便于回答「我一共问了几个」这类元问题）；普通税法/劳动法问题在未触发「自述历史」规则时 **不会** 附带历史片段。

---

## 8. 离线数据流水线（首次必跑）

在项目根目录、已激活 Conda：

```bash
python offline/scripts/run_full_offline.py
```

或分步：`run_mysql_ingest.py` → `run_milvus_sync.py`。  
确保 `data/` 下有所需 **FAQ Excel**（默认文件名见 `mysql_loaders`）与 **PDF**。  
（说明：离线流水线**不负责**导入 `users_tab`/`his_chat_tab`，该部分由在线对话自动写入。）

---

## 9. 启动后端 API

```bash
uvicorn backend.app.main:app --host 0.0.0.0 --port 8000
```

生产建议 **`--workers 1`** 或与文档一致的多进程策略，避免每进程重复加载大模型；横向扩展宜拆向量服务。

---

## 10. 启动前端（开发）

```bash
cd frontend
npm install
npm run dev
```

浏览器访问 Vite 提示端口（默认 `5173`）；`/api` 由 Vite 代理到 `127.0.0.1:8000`。

---

## 11. 健康检查

`GET /health` 返回服务状态；对话使用 `POST /api/chat/stream`（SSE）。

---

## 12. 测试

```bash
pytest
```

测试目录为 `offline/tests`（见 `pyproject.toml`），其中包含分块逻辑与 **`test_history_detect`**（记忆意图正则）等。
