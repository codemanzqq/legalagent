# 四个项目并发性能与安全评估报告

---

## 一、RAG 智能客服问答系统（Legal_System）

### 1. 并发性能评估

#### 架构特点
- **框架**：FastAPI (Python) + Uvicorn ASGI 服务器
- **并发模型**：异步 IO (asyncio) + 协程
- **线程池**：内置 asyncio 线程池（用于 CPU 密集操作）
- **连接池**：
  - MySQL：`get_async_engine()` 使用 SQLAlchemy 异步连接池（默认池大小 5-10）
  - Milvus：PyMilvus 连接池（默认配置）
  - Redis：异步客户端（无池化，单连接复用）

#### 并发瓶颈分析
| 组件 | 瓶颈点 | 影响 |
|------|--------|------|
| Embedding | 本地 BGE-M3 模型，CPU 推理，单请求串行 | 每请求 50-200ms，高并发时排队 |
| Rerank | 本地 CrossEncoder，CPU 推理 | 每请求 100-300ms，TopK 越大越慢 |
| Milvus | 向量检索 TopK=60，混合检索双路召回 | 单查询约 30-80ms |
| LLM | 调用阿里云 DashScope API | 依赖外部服务 QPM 限制 |
| MySQL | 异步连接池默认 5-10 | 高并发时连接等待 |

#### 预估并发能力
- **开发环境（本机 CPU 推理）**：QPS ≈ 5-15，并发用户 10-30
- **生产环境（GPU+ 分布式部署）**：QPS ≈ 50-150，并发用户 100-300
- **理论上限**：受限于 Embedding/Rerank 模型吞吐，单机约 200 QPS（需 A10/A100）

#### 优化建议
1. Embedding/Rerank 模型部署到独立 GPU 服务，使用 Triton 推理服务器
2. Milvus 集群部署，分片 + 副本提升向量检索吞吐
3. Redis 缓存热点问答（当前 TTL=3600s），缓存命中率目标>60%
4. 异步连接池扩容：`pool_size=20, max_overflow=40`
5. 引入请求限流（如 `slowapi`），保护 LLM API 配额

---

### 2. 安全性评估

#### 已实现的安全措施
| 安全域 | 实现情况 | 风险等级 |
|--------|----------|----------|
| CORS | 白名单配置（`cors_origins`），允许本地开发端口 | 低 |
| SQL 注入 | SQLAlchemy ORM 参数化查询 | 低 |
| XSS | FastAPI 默认转义响应，前端 Vue 自动过滤 | 低 |
| 认证授权 | **缺失**：无用户登录/Token 验证 | **高** |
| API 限流 | **缺失**：无速率限制 | 中 |
| 敏感信息 | .env 文件读取，密码字段校验（非负阈值） | 中 |
| 日志审计 | 基础日志，无敏感信息脱敏 | 中 |

#### 高危漏洞
1. **无认证机制**：任何用户可调用 `/api/chat/stream` 接口，LLM API 费用无保护
2. **无权限控制**：Milvus/MySQL 数据可被任意查询
3. **无速率限制**：恶意用户可高频请求导致 API 配额耗尽

#### 修复建议（优先级排序）
1. 添加 JWT 认证中间件（参考 Policy Agent 项目）
2. 引入 `slowapi` 或 `fastapi-limiter` 实现 API 限流
3. 敏感配置（MySQL 密码、DashScope Key）加密存储
4. 日志脱敏：隐藏用户查询内容、Token、密码
5. 添加 SQL 注入防护层（ORM 已足够，但需审计原生查询）

**安全评级**：D（开发环境可用，生产环境需立即加固）

---

## 二、DataAgent 企业级智能数据分析 Agent

### 1. 并发性能评估

#### 架构特点
- **框架**：Spring Boot 3.4.8+ (Java 17+)
- **并发模型**：Servlet 容器（Tomcat）+ Spring 异步执行器
- **核心组件**：
  - StateGraph 工作流（16+ 节点编排）
  - Python 代码执行器池（Docker/Local/AI-Sim）
  - 向量检索（pgvector/可选混合检索）
  - 多模型调度（动态切换 LLM/Embedding）

#### 并发配置（基于代码分析）
```java
// Python 执行器池（CodePoolExecutorService）
- Docker 模式：容器池大小可配置（默认 5-10）
- Local 模式：线程池大小 = CPU 核心数
- AI-Sim 模式：无池化，串行执行

// 图执行（GraphServiceImpl）
- 每个请求占用 1 个 StateGraph 实例
- 节点执行支持异步（@Async 配置）
- 多轮对话上下文缓存（Redis）
```

#### 并发瓶颈分析
| 组件 | 瓶颈点 | 影响 |
|------|--------|------|
| StateGraph | 单请求独占工作流实例 | 复杂查询耗时 5-30 秒 |
| Python 执行器 | Docker 容器启动慢（2-5 秒） | 高并发时容器池耗尽 |
| Text-to-SQL | 语义校验 + 执行验证双阶段 | 增加 500-1000ms 延迟 |
| 向量检索 | pgvector TopK 检索 | 单查询 50-200ms |
| 报告生成 | ECharts 图表渲染 + HTML 生成 | 增加 1-3 秒延迟 |

#### 预估并发能力
- **开发环境**：QPS ≈ 10-30，并发用户 20-50
- **生产环境（K8s 集群）**：QPS ≈ 100-300，并发用户 200-500
- **理论上限**：受限于 Python 执行器池大小，单机约 50 并发任务

#### 优化建议
1. Python 执行器预 warm 容器池（启动时初始化 5-10 个空容器）
2. StateGraph 节点异步化（Planner/SQL 生成/报告生成可并行）
3. 引入结果缓存（相似查询直接返回历史分析结果）
4. pgvector 索引优化（HNSW 替代 IVFFlat，提升检索 3-5 倍）
5. 报告生成异步化（生成完成后回调通知用户）

---

### 2. 安全性评估

#### 已实现的安全措施
| 安全域 | 实现情况 | 风险等级 |
|--------|----------|----------|
| API Key 管理 | 完整生命周期管理（加密存储、权限控制） | 低 |
| MCP 协议 | 原生支持，工具调用鉴权 | 低 |
| SQL 注入 | Text-to-SQL 语义校验 + 参数化执行 | 低 |
| Python 执行 | 沙箱隔离（Docker 模式） | 低 |
| 文件上传 | 类型校验 + 大小限制 | 低 |
| 日志审计 | Langfuse 集成（可选） | 中 |
| 认证授权 | **依赖外部系统**（无内置登录） | 中 |

#### 潜在风险
1. **Python 代码注入**：Local 模式下用户代码可直接访问服务器文件系统
2. **资源耗尽**：恶意用户提交死循环代码（`while True`）占用执行器
3. **数据泄露**：多租户数据隔离依赖 API Key，无强制行级权限
4. **MCP 工具滥用**：nl2SqlTool 可查询任意表（需外部鉴权）

#### 修复建议
1. 生产环境强制使用 Docker 执行器模式，禁用 Local 模式
2. 添加代码执行超时（默认 30 秒）+ CPU/内存配额限制
3. 实现多租户行级权限（基于 agentId 过滤数据源）
4. MCP 工具调用添加二次鉴权（如 OAuth2 Scope 校验）
5. 敏感操作审计日志（SQL 执行、文件上传、模型调用）

**安全评级**：B+（企业级功能完备，需补充租户隔离与执行沙箱）

---

## 三、山东省智能政策咨询助手（Policy Agent）

### 1. 并发性能评估

#### 架构特点
- **框架**：Spring Boot 3.4.1 (Java 21)
- **并发模型**：Servlet 容器（Tomcat）+ 异步执行器
- **核心组件**：
  - ChatAsyncConfig：聊天异步线程池（核心 2、最大 6、队列 200）
  - KnowledgeIngestAsyncConfig：知识入库异步线程池
  - Redis 会话缓存（多轮对话上下文）
  - PostgreSQL + pgvector 向量检索

#### 并发配置（基于代码分析）
```java
// ChatAsyncConfig
- corePoolSize: 2
- maxPoolSize: 6
- queueCapacity: 200
- 线程名前缀："chat-async-"

// 知识入库（KnowledgeIngestAsyncConfig）
- 独立线程池，避免阻塞聊天请求

// 向量检索
- pgvector HNSW 索引（需手动创建）
- TopK 可配置（默认 5-10）
```

#### 并发瓶颈分析
| 组件 | 瓶颈点 | 影响 |
|------|--------|------|
| 聊天线程池 | maxPoolSize=6，队列 200 | 第 7 个请求排队，队列满后拒绝 |
| 向量检索 | pgvector 串行查询 | 单请求 50-150ms |
| Tool 调用 | webSearch/补贴计算串行执行 | 增加 500-2000ms 延迟 |
| Redis 缓存 | 单实例，无集群 | 会话读写 5-20ms |
| LLM 调用 | DashScope API 限流 | 依赖配额（如 100 QPM） |

#### 预估并发能力
- **开发环境**：QPS ≈ 15-40，并发用户 30-80
- **生产环境**：QPS ≈ 80-200，并发用户 150-400
- **理论上限**：受限于聊天线程池（6 线程）+ LLM API QPM

#### 优化建议
1. 聊天线程池扩容：`corePoolSize=10, maxPoolSize=30, queueCapacity=500`
2. Tool 调用并行化（webSearch 与补贴计算可同时发起）
3. Redis 集群部署（主从 + 分片），提升会话缓存吞吐
4. pgvector 索引优化（HNSW `m=16, efConstruction=200`）
5. 引入熔断器（Resilience4j）保护 LLM/API 调用

---

### 2. 安全性评估

#### 已实现的安全措施
| 安全域 | 实现情况 | 风险等级 |
|--------|----------|----------|
| JWT 认证 | 完整实现（JwtService + JwtAuthenticationFilter） | 低 |
| 密码加密 | BCrypt（Salt + 多轮哈希） | 低 |
| CORS | 动态配置（`app.security.cors.allowed-origin-patterns`） | 低 |
| CSRF | 禁用（ Stateless Session + JWT） | 低 |
| 权限控制 | 基于角色（`hasRole("ADMIN")`） | 低 |
| 异常处理 | 统一 401/403 JSON 响应 | 低 |
| 日志审计 | LoggingAdvisor（可集成 Langfuse） | 中 |

#### JWT 配置分析
```java
// JwtService
- secretKey: 可配置（默认值仅开发使用）
- expiration: 86400000ms（24 小时）
- 算法：HS256（HmacSHA256）
```

#### 潜在风险
1. **JWT 密钥弱默认值**：生产环境未修改默认密钥可被爆破
2. **Token 刷新机制缺失**：24 小时有效期过长，泄露后风险窗口大
3. **Tool 失败策略**：ToolFailurePolicyCenter 统一处理，但无重试次数限制
4. **文件上传**：多模态分析支持发票/设备图片，无文件大小/类型白名单

#### 修复建议
1. 生产环境强制修改 JWT 密钥（Base64 编码，长度>64 字符）
2. 引入 Refresh Token 机制（Access Token 15 分钟，Refresh Token 7 天）
3. Tool 重试次数限制（默认 3 次）+ 退避策略（指数退避）
4. 文件上传校验（类型白名单：JPG/PNG，大小限制<5MB）
5. 添加 SQL 注入防护（JPA 已足够，但需审计原生查询）

**安全评级**：A-（生产级认证授权，需补充 Token 刷新与文件校验）

---

## 四、智能食谱生成器（Recipe Planner）

### 1. 并发性能评估

#### 架构特点
- **框架**：Spring Boot 3.2 (Java 17)
- **并发模型**：Servlet 容器（Tomcat）+ 标准线程池
- **核心组件**：
  - JWT 认证过滤器（每请求校验）
  - Claude API 调用（食谱生成 + 聊天助手）
  - PostgreSQL 存储（用户/食谱/聊天数据）
  - PDF/Word 导出（CPU 密集）

#### 并发配置（基于代码分析）
```java
// SecurityConfig
- STATELESS Session（无服务器端会话）
- JWT 过滤器每请求执行

// 无自定义线程池配置（依赖 Tomcat 默认）
- Tomcat 默认：maxThreads=200, minSpareThreads=10
```

#### 并发瓶颈分析
| 组件 | 瓶颈点 | 影响 |
|------|--------|------|
| Claude API | 外部服务，延迟 1-5 秒 | 高并发时线程占用 |
| PDF/Word 导出 | CPU 密集，单请求 2-10 秒 | 占用 Tomcat 工作线程 |
| JWT 校验 | 每请求 HS256 验签 | <1ms，可忽略 |
| PostgreSQL | 连接池（HikariCP 默认 10） | 高并发时连接等待 |

#### 预估并发能力
- **开发环境**：QPS ≈ 20-50，并发用户 40-100
- **生产环境**：QPS ≈ 100-250，并发用户 200-500
- **理论上限**：受限于 Claude API 延迟 + Tomcat 线程池（200 线程）

#### 优化建议
1. PDF/Word 导出异步化（提交任务→轮询完成→下载）
2. Tomcat 线程池扩容：`maxThreads=500, minSpareThreads=50`
3. HikariCP 连接池扩容：`maximumPoolSize=30, minimumIdle=10`
4. 食谱生成结果缓存（相同用户参数 7 天内直接返回）
5. 引入异步非阻塞 IO（WebFlux）替代部分 Servlet 阻塞调用

---

### 2. 安全性评估

#### 已实现的安全措施
| 安全域 | 实现情况 | 风险等级 |
|--------|----------|----------|
| JWT 认证 | 完整实现（JwtService + JwtAuthenticationFilter） | 低 |
| 密码加密 | BCrypt（Spring Security 默认） | 低 |
| CORS | 未配置（依赖 Spring Boot 默认） | 中 |
| CSRF | 禁用（STATELESS Session） | 低 |
| 权限控制 | 基础角色（USER/ADMIN） | 低 |
| SQL 注入 | JPA 参数化查询 | 低 |
| XSS | React 默认转义 | 低 |

#### JWT 配置分析
```java
// JwtService
- jwtSecret: 配置文件读取（无默认值）
- jwtExpirationMs: 可配置（默认未设置）
- refreshExpirationMs: 可配置（支持 Refresh Token）
- 算法：HS256（HmacSHAKeyFor）
```

#### 潜在风险
1. **CORS 未显式配置**：生产环境可能允许跨域请求
2. **JWT 密钥管理**：配置文件明文存储，无加密
3. **文件导出接口**：PDF/Word 导出无访问频率限制
4. **聊天助手**：无内容审核，可能生成不当内容
5. **用户数据隔离**：仅靠 user_id 过滤，无行级安全策略

#### 修复建议
1. 显式配置 CORS 白名单（参考 Policy Agent）
2. JWT 密钥加密存储（如 Jasypt 加密配置文件）
3. 导出接口限流（每用户每分钟最多 3 次）
4. 聊天内容过滤（关键词过滤 + Claude 内容审核 API）
5. 实现行级安全（PostgreSQL RLS 或 JPA 过滤器）

**安全评级**：B+（认证授权完整，需补充 CORS 与内容审核）

---

## 五、综合对比与推荐

### 并发性能对比

| 项目 | 框架 | 并发模型 | 预估 QPS | 并发用户 | 主要瓶颈 |
|------|------|----------|---------|---------|---------|
| RAG 客服 | FastAPI | 异步 IO | 50-150 | 100-300 | Embedding/Rerank 推理 |
| DataAgent | Spring Boot | 线程池+StateGraph | 100-300 | 200-500 | Python 执行器池 |
| Policy Agent | Spring Boot | 线程池+Redis | 80-200 | 150-400 | 聊天线程池 (6 线程) |
| Recipe Planner | Spring Boot | Tomcat 线程池 | 100-250 | 200-500 | Claude API 延迟 |

### 安全性对比

| 项目 | 认证 | 授权 | 数据加密 | 限流 | 审计 | 综合评级 |
|------|------|------|----------|------|------|---------|
| RAG 客服 | ❌ | ❌ | ⚠️ | ❌ | ⚠️ | D |
| DataAgent | ⚠️ (API Key) | ✅ | ✅ | ⚠️ | ✅ | B+ |
| Policy Agent | ✅ (JWT) | ✅ (RBAC) | ✅ (BCrypt) | ⚠️ | ⚠️ | A- |
| Recipe Planner | ✅ (JWT) | ⚠️ (基础) | ✅ (BCrypt) | ❌ | ❌ | B+ |

### 生产环境部署建议

#### RAG 客服（Legal_System）
**优先级：安全加固 > 并发优化**
1. 立即添加 JWT 认证（可复用 Policy Agent 代码）
2. 引入 API 限流（slowapi，每 IP 每分钟 60 请求）
3. Embedding/Rerank 模型 GPU 部署（Triton 推理服务器）
4. Redis 缓存扩容（集群模式，缓存 TTL 动态调整）

#### DataAgent
**优先级：执行沙箱 > 租户隔离 > 并发优化**
1. 强制 Docker 执行器模式，禁用 Local 模式
2. 实现多租户行级权限（基于 agentId 过滤）
3. Python 执行器预 warm 容器池
4. 添加结果缓存（相似查询复用历史分析）

#### Policy Agent
**优先级：Token 刷新 > 线程池扩容 > 文件校验**
1. 引入 Refresh Token 机制（Access Token 15 分钟）
2. 聊天线程池扩容（maxPoolSize=30）
3. 文件上传类型/大小白名单校验
4. Tool 重试次数限制（3 次）+ 指数退避

#### Recipe Planner
**优先级：CORS 配置 > 导出限流 > 内容审核**
1. 显式配置 CORS 白名单
2. PDF/Word 导出异步化 + 限流
3. JWT 密钥加密存储（Jasypt）
4. 聊天内容过滤（关键词 + API 审核）

---

## 六、总结

### 最安全项目：Policy Agent（A-）
- JWT 认证完整，RBAC 权限控制清晰
- 异常处理统一，CORS 动态配置
- 需补充：Token 刷新机制、文件校验

### 并发最强项目：DataAgent（100-300 QPS）
- StateGraph 工作流编排，支持异步节点
- Python 执行器池化，容器隔离
- 需补充：执行沙箱、租户隔离

### 风险最高项目：RAG 客服（D）
- 无认证、无限流、无审计
- 开发环境可用，生产环境需立即加固
- 建议优先复用 Policy Agent 的 JWT 认证模块

### 综合能力评估
四个项目涵盖 RAG、Agent 工作流、Tool 集成、全栈应用等场景，技术栈横跨 Python(FastAPI) 与 Java(Spring Boot)。生产部署前需针对性补齐安全短板，并发优化可根据实际压测结果逐步调优。
