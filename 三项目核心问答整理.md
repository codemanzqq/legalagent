# 三项目核心问答整理

## 一、Legal RAG - 智能客服问答系统

### 技术栈
- **后端**: FastAPI (Python) + LangChain
- **向量库**: Milvus
- **Embedding**: 本地 bge-m3
- **Rerank**: 本地 CrossEncoder (bge-reranker-large)

### 核心问答

| 问题 | 答案 |
|------|------|
| RAG 分块方法？ | **父子分块**：父块 1800 字符（合并 PDF 页），子块 512 字符（滑动窗口，重叠 128） |
| 检索策略？ | BM25 + 向量混合检索，RRF 融合，CrossEncoder 重排 |
| AI 框架？ | LangChain RAG 管线，本地 HuggingFace 模型 |

---

## 二、Policy Agent - 山东省以旧换新系统

### 技术栈
- **后端**: Spring Boot 3.4.1 + Java 21
- **AI 框架**: Spring AI 1.0.3 (DashScope API)
- **向量库**: PostgreSQL + pgvector
- **Embedding/Rerank**: 阿里云 DashScope (text-embedding-v3 / qwen3-vl-rerank)

### 核心问答

| 问题 | 答案 |
|------|------|
| RAG 分块方法？ | **单层语义分块**：900 字符，重叠 150，段落→句子→字符回退 |
| 嵌入模型动态路由？ | 知识库绑定 `embedding_model` 字段 → 运行时读取 → 路由到对应向量表 |
| 混合检索实现？ | 向量 (余弦 65%) + 关键词 (BM25 35%) → RRF 融合 → DashScope Rerank |
| Rerank 可选含义？ | **配置驱动可选**：三层回退 - 开关→API Key→错误捕获 |
| ToolFailurePolicyCenter？ | 统一重试 (2 次/300ms)→缓存降级→兜底提示，无效调用↓40% |
| AI 框架？ | Spring AI ChatClient + ReAct 工具集成 + ToolFailurePolicyCenter |

---

## 三、DataAgent - 企业级智能数据分析 Agent

### 技术栈
- **后端**: Spring Boot 3.4.8+ + Java 17+
- **AI 框架**: Spring AI Alibaba Graph (StateGraph 工作流)
- **向量库**: pgvector / Milvus (可选)
- **特色**: Text-to-SQL + Python 代码执行 + MCP 协议

### 核心问答

| 问题 | 答案 |
|------|------|
| RAG 分块方法？ | **5 种分块器可选**：TOKEN / RECURSIVE / SENTENCE / PARAGRAPH / SEMANTIC |
| 分块器可选体现？ | 前端下拉选择，helper text 显示各策略说明 |
| Human-in-the-loop？ | StateGraph 编译时 `interruptBefore(HUMAN_FEEDBACK_NODE)` → PlanExecutorNode 路由 → 用户审批/驳回 |
| 动态 LLM 路由？ | **手动配置切换**，非自动路由；`AiModelRegistry` 懒加载 `refreshChat()` 热更新 |
| MCP 协议支持？ | 作为 Tool Server 对接 Claude Desktop，提供 NL2SQL 能力 |

---

## 三项目对比

| 维度 | Legal RAG | Policy Agent | DataAgent |
|------|-----------|--------------|-----------|
| **语言** | Python | Java | Java |
| **AI 框架** | LangChain | Spring AI | Spring AI Alibaba Graph |
| **RAG 分块** | 父子 (1800→512) | 单层语义 (900) | 5 种可选 |
| **Rerank** | 本地 CrossEncoder | DashScope API | 可选 |
| **检索** | BM25+ 向量 | 向量 + 关键词+RRF | 向量 |
| **部署** | 本地模型 | 云端 API | 多模型注册表 |
| **核心场景** | 文档问答 | 政策咨询 + 工具调用 | Text-to-SQL+Python 分析 |
