## 1.mysql

小皮启动

## 2.docker

### 2.1.打包redis/milvus相关镜像（推荐）

#### 1、打包

docker save -o milvus-deps.tar `
  docker.m.daocloud.io/library/redis:7-alpine `
  quay.io/coreos/etcd:v3.5.5 `
  docker.m.daocloud.io/minio/minio:RELEASE.2023-03-20T20-16-18Z `
  docker.m.daocloud.io/milvusdb/milvus:v2.4.6



#### 2、解压

cd deploy

docker load -i milvus-deps.tar 

docker images       #查看镜像是否有镜像

#### 3、启动redis/milvus等容器服务

cd deploy
docker compose up -d





### 2.2.没有打包redis/milvus相关镜像，需要连外网

根目录下：

docker compose pull

docker compose up -d



## 3.excal-->mysql-->milvus

1.切换到conda环境

2.python offline\scripts\run_mysql_ingest.py

3.python offline\scripts\run_milvus_sync.py



## 4.启动后端

1.切换到conda环境

2.项目根目录

3.uvicorn backend.app.main:app --host 0.0.0.0 --port 8000



## 5.启动前端

1.cd D:\xiaoyi\Legal_System\frontend
2.npm install

3.npm run dev

4.访问http://localhost:5173/



